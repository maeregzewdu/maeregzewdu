---
deployment:
  tasks:
    - export DEPLOYPATH=/home/maeregqx/public_html
    - /bin/mkdir -p $DEPLOYPATH
    - /bin/cp -R * $DEPLOYPATH
    - cd $DEPLOYPATH && /usr/local/cpanel/3rdparty/bin/composer install --no-dev
    - chmod -R 755 $DEPLOYPATH/storage
